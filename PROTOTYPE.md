# 테니스 밸런스 프로토타입

Unity에서 `Assets/01_Scenes/BalancePrototype.unity`를 열고 Play를 누르세요.
메뉴 `Tennis > Open Balance Prototype`으로도 열 수 있습니다. 빌드의 시작 씬도 이 씬으로 연결했습니다.
코트와 캐릭터는 실행 시 Cube/Capsule/Sphere로 생성합니다. 기본 효과음은 코드에서 합성합니다.

## 조작

| 입력 | 동작 |
|---|---|
| WASD | 이동 |
| 왼쪽 Shift | 스태미나를 소모하며 달리기 |
| 방향키 | 좌우/깊이 조준 (위: 깊게, 아래: 짧게) |
| Space 누르고 떼기 | 충전 후 타격/서브 |
| 1 / 2 / 3 / 4 / 5 | 플랫 / 로브 / 드롭 / 슬라이스 / 탑스핀 |
| Esc / R / F1 | 일시정지 / 경기 재시작 / 밸런스 패널 |

자기 서브에서는 위치가 고정됩니다. SPACE를 누른 채 방향키로 조준한 후 떼세요.
랠리에서는 공이 캐릭터의 타격 범위에 들어올 때 SPACE를 떼세요. 0.22초의 입력 버퍼가 있습니다.
캐릭터 가까이서 받아치면 Perfect, 범위 가장자리에서는 Nice와 더 큰 착지 오차가 적용됩니다.
서브는 반드시 한 번 바운드한 뒤 받을 수 있습니다. 랠리에서는 발리도 가능합니다.
스킬은 스태미나와 재사용 대기시간을 소모합니다. 사용할 수 없으면 플랫으로 타격합니다.

## 난이도와 수치

화면 오른쪽에서 Easy / Normal / Hard를 즉시 선택할 수 있습니다.
Custom에서는 AI 이동/타구 속도, 반응 지연, 착지점 예측 오차, 타구별 실수 확률,
상대 반대 방향으로 공격하는 정도, 스킬 사용 확률을 조절할 수 있습니다.
AI는 순간이동하지 않고 코트의 앞뒤와 좌우로 이동합니다. 반응/실수 판정은 다음 상대 타구부터 적용됩니다.
Player 이동/타구 속도와 타격 허용 범위도 화면에서 조절할 수 있습니다.

영구 기본값은 `Assets/PrototypeBalance.asset`의 Inspector에서 편집하세요.
실행 중 설정은 이 애셋의 복사본을 사용하므로 Play 종료 후 원래 값으로 돌아옵니다.
경기 길이, 스태미나, 충전 보너스, 스킬 비용/쿨다운, 바운드도 같은 애셋에서 설정합니다.
세트 목표 게임 수와 승리 세트 수는 재시작할 때 적용됩니다.
기본 규칙은 6게임, 두 게임 차, 6-6 타이브레이크(7점/두 점 차), 2세트 선승입니다.
서브는 매 게임 교대하고 매 포인트 좌우를 바꿉니다. 타이브레이크는 첫 1점 뒤, 이후 2점마다 교대합니다.
네트/아웃/두 번 바운드/더블 폴트로 득점합니다. 네트 윗부분을 스친 유효 서브는 렛으로 재시도합니다.

## 원본과 구현 범위

참고한 자료는 Downloads의 역컴파일 프로젝트에 있는 `BotAIStatsConfig`, `StatsModifier`,
`Ball`, `PlayerController`, `BotAIController`, `ScoreManager`입니다.
원본의 충전, 스태미나, 타격 판정, 샷별 궤도와 바운드, AI 반응/배치, 경기 점수를 참고해 새로 구현했습니다.
csproj는 참조 목록이며 실제 원본 씬/설정 애셋은 제공된 코드에 없습니다.
따라서 원본 수치나 조작감을 그대로 복원한 완성본이 아닌 핵심 경기를 실험하는 프로토타입입니다.
캠페인, 상점, 장비 성장, 특수 원소 스킬, 연출/스토리 화면은 구현하지 않았습니다.

기존 `MainScene`과 기존 컨트롤러 파일은 보존했습니다.
새 코드는 `Assets/02_Scripts/Prototype`의 별도 namespace로 동작합니다.
나중에 캐릭터/라켓 모델은 `PrototypeMatch.BuildCourt`와 `AttachRacket`의 생성 부분을 프리팹으로 교체하고,
효과음은 합성 클립 대신 AudioClip으로 교체할 수 있습니다.

점수 규칙 검사: `dotnet run --project Tools/PrototypeChecks/PrototypeChecks.csproj`

검증 결과: Unity DLL 기준 컴파일 오류/경고 0개, 점수 규칙 검사 17개 통과.
별도 임시 Unity 6000.6.0f1 프로젝트의 Play Mode에서 기본 도형 생성, 카메라/오디오 초기화,
설정 복사, 자기 타구 재타격 차단, 서브 발리 차단, 바운드 득점, 중복 득점 차단,
첫 폴트 재시도, 더블 폴트 득점, 재시작을 포함한 실행 검사 10개가 통과했습니다.
이 실행 검사는 그래픽 없는 모드에서 수행했으며 화면 외관과 실제 손 조작감은 별도 확인이 필요합니다.
